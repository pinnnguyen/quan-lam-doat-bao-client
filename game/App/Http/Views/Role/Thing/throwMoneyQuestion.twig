{% extends "Base/parent.twig" %}

{% block body %}
    Ngươi muốn vứt bỏ bao nhiêu tiền? (1 vì một văn đồng tiền, 100 vì một lượng bạc trắng, 10000 vì một lượng vàng )
    <form action="{{ url(request, role_thing.throwUrl) }}" method="post">
        <input type="text" name="number" min="1">
        <br/>
        <input type="submit" value="Xác định vứt bỏ">
    </form>
    <a href="{{ url(request, role_thing.backUrl) }}">Trở lại</a>
    <br/>
    <a href="{{ url(request, 'Map/Index/index') }}">Trở lại trò chơi</a>
{% endblock %}